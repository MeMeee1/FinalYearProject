{
  "name": "nitro-app",
  "private": true,
   "scripts": {
    "build": "prisma generate --schema=./prisma/schema.prisma && nitro build",
    "dev": "prisma generate --schema=./prisma/schema.prisma && nitro dev",
    "preview": "node .output/server/index.mjs",
    "postinstall": "prisma generate --schema=./prisma/schema.prisma"
  },
  "devDependencies": {
    "h3": "^1.15.4",
    "nitropack": "^2.12.4"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1",
    "expoecommerce_finalproject": "file:../..",
    "patch-package": "^8.0.1",
    "prisma": "^6.19.1"
  }
}
